# Locked Technical Decisions

One-line summary of each non-negotiable choice, with a link to the full ADR. Lazy-referenced from `CLAUDE.md`.

When you touch any row, also update the corresponding ADR (never the other way around — ADRs are the source of truth).

| Decision | Choice | Why | ADR |
|---|---|---|---|
| Package manager | `uv` | Fast, reproducible, pyproject.toml-native | [0011](../decisions/0011-uv-package-manager.md) |
| Hotkey | Scroll Lock, single-tap toggle | Non-printable; will not collide with typing | [0001](../decisions/0001-scroll-lock-hotkey.md) |
| Verb router | `VerbRouter` with `VerbRule` / `SubcommandRule` / `RouteTarget` dataclasses; deterministic verb router; matches command/workflow names + synonyms first (longest match wins, underscore↔space), then primitive verb rules. rapidfuzz is no longer in the routing path — used only by resolver.resolve_window/resolve_app for focus/open target grounding (ADR 0041). | ~1 ms routing for core commands; eliminates LM Studio dependency for 90% of utterances | [0074](../decisions/0074-merlin-gated-verb-router.md) |
| Audio capture | `sounddevice`, device-native rate, mono float32 | `InputStream` opens at device rate (e.g. 48 kHz WASAPI); raw frames pushed to `raw_q` | [0003](../decisions/0003-sounddevice-over-pyaudio.md) |
| Resampler | `soxr.ResampleStream`, device-native → 16 kHz, HQ | True streaming resampler with internal filter state; one instance per session | [0017](../decisions/0017-soxr-streaming-resampler.md) |
| VAD engine | `silero-vad` ONNX, 512-sample 16 kHz frames | Neural VAD with float confidence; robust to keyboard/fan noise | [0016](../decisions/0016-silero-vad-over-webrtcvad.md) |
| VAD gate | `VADGate` — pre-roll buffer + utterance accumulation + max-utterance guard | Pre-roll captures onset; max-utterance bounds buffer growth | [0015](../decisions/0015-vad-streaming-mode.md) |
| Session model | Scroll Lock toggles session; optional mute key suspends/resumes stream within a session (two flags: `session_active`, `muted`, plus monotonic `_audio_gen` counter for in-flight stale-utterance guard) | VAD auto-segments between keypresses; mute coexists with external dictation apps; gen counter closes the transcribe-vs-mute race that boolean `_muted` could not | [0015](../decisions/0015-vad-streaming-mode.md), [0025](../decisions/0025-mute-hotkey-for-external-dictation.md) (live; ADR 0072 supersession reverted with ADR 0082; gen-counter amendment 2026-05-15) |
| Transcription | `faster-whisper` `small.en` on CUDA; ndarray hand-off | Sub-second latency on NVIDIA GPU; skips temp-file I/O on the hot path | [0004](../decisions/0004-faster-whisper-cuda.md), [0018](../decisions/0018-ndarray-handoff-to-whisper.md) |
| Transcription backend toggle | `[transcription] backend = "local" \| "remote"`; `RemoteTranscriber` POSTs WAV multipart to whisper.cpp `/inference` with `verbose_json`; failure → miss chime | Lets daemon offload Whisper to a LAN box (frees local GPU) without changing the rest of the pipeline; same confidence math; degrades silently | [0073](../decisions/0073-remote-transcription-backend.md) *(superseded by ADR 0075)* |
| Routing strategy | `VerbRouter` is the sole routing path; LLM router removed (ADR 0082) | Eliminates LM Studio dependency; deterministic, ~1 ms routing | [0076](../decisions/0076-registry-aware-verb-router.md), [0082](../decisions/0082-remove-llm-routing-path.md) *(supersedes 0074)* |
| Fuzzy match scope | `rapidfuzz` retained for **parameter resolution only** (`focus(target)` / `open(target)` grounding inside `resolver.py`); removed from routing | `WRatio` is the right scoring function for fuzzy tool arguments; reimplementing it would be more code than the dependency costs | [0041](../decisions/0041-rapidfuzz-for-parameter-resolution.md) |
| Tool registry | `@tool` decorator + auto-discovery; sidecar `.toml` per group | Phrases live next to code; metadata hot-reloadable without restart | [0006](../decisions/0006-tool-decorator-registry.md), [0021](../decisions/0021-sidecar-toml-per-tool.md), [0024](../decisions/0024-bare-tool-decorator-migration.md) |
| Feedback | `winsound` miss chimes only (no visual toasts, no success chime) | Fire-and-forget, non-interruptive. WinRT toasts dropped for UX + CUDA-init fragility. | [0007](../decisions/0007-windows-toasts-over-tkinter.md), [0008](../decisions/0008-winsound-for-chimes.md), [0013](../decisions/0013-drop-winrt-toasts-audio-only-feedback.md), [0014](../decisions/0014-miss-only-chimes.md) |
| Debug artifact | `outputs/last_utterance.wav` — async overwrite per utterance | Fire-and-forget; post-mortem inspection only | [0018](../decisions/0018-ndarray-handoff-to-whisper.md), [0019](../decisions/0019-supersede-single-shot-recorder.md) |
| Config | Single `config.toml` tracked in git; `[llm]` section silently dropped with deprecation warning (ADR 0082) | One source of truth; migration shim preserves backward compat for existing config files | *(spec §5)* |
| CUDA DLL loading | `nvidia-cublas-cu12` + `nvidia-cudnn-cu12` pip wheels + `_cuda_setup.register()` preloads DLLs via `ctypes.WinDLL` | Sidesteps Windows DLL-search / stale-PATH quirks so every entry point loads the same DLLs. **System CUDA Toolkit 12.x + cuDNN 9.x MSI install on `PATH` is still required** (wheels supplement, do not replace). | [0012 (amended)](../decisions/0012-cuda-dll-bundling.md) |
| Threading model | Four long-lived threads (PortAudio callback → VAD worker → pipeline worker, plus hotkey listener) connected by `queue.Queue` | PortAudio callback has a real-time deadline; cross-thread work dispatched via queues only | [0010](../decisions/0010-threading-model.md) |
| Phased delivery | Each phase ends in an automated test gate **and** a human validation gate | No feature is "done" until both pass | [0009](../decisions/0009-phased-delivery-with-hitl-gates.md) |
| Web UI | Embedded FastAPI + HTMX on a uvicorn daemon thread | Zero build step; partial-page updates; hot-reloads registry metadata | [0020](../decisions/0020-web-ui-embedded-fastapi.md), [0022](../decisions/0022-htmx-over-spa.md), [0023](../decisions/0023-metadata-only-hot-reload.md) |
| Schema-driven kwargs form | `ToolEntry.args_meta` + `GET /command/kwargs-form` HTMX fragment + `kwargs_mode=guided\|advanced` POST field | Registry holds schema; dedicated endpoint swaps only the kwargs section; two modes preserve backwards compat | [0057](../decisions/0057-schema-driven-kwargs-form.md) |
| Process supervision | Python supervisor owns daemon + sprite lifecycle; daemon restarts via graceful exit 75 (`request_restart`) | Survives daemon restarts; sprite stays on screen; deprecates detach-spawn `schedule_restart` | [0058](../decisions/0058-supervisor-process-owns-lifecycle.md) |
| Primitives-only MVP | Tool catalog reduced to 11 primitives (7 action + 4 perception). `no_match` removed (ADR 0082). VerbRouter routes primitive verbs AND user-authored commands/workflows by name; semantic shortcuts (copy, close, etc.) are user-authored graphs. speak/RemoteTranscriber/Summarizer rule-table all removed. | Minimal orthogonal catalog; user composes higher-level commands in the Builder | [0075](../decisions/0075-primitives-only-mvp.md), [0082](../decisions/0082-remove-llm-routing-path.md) |

## ~~LLM Router~~ (removed — ADR 0082)

> **All entries in this section are superseded by [ADR 0082](../decisions/0082-remove-llm-routing-path.md).** The LLM routing path (Merlin toggle, LM Studio, prompt template, no_match, warmup) has been removed from the codebase. The table is retained for historical context only.

| Decision | Choice | Why | ADR |
|---|---|---|---|
| ~~Routing strategy~~ | ~~LLM-only when Merlin mode is active~~ | Superseded by ADR 0082 | [0074](../decisions/0074-merlin-gated-verb-router.md) *(superseded)* |
| ~~Hybrid routing~~ | ~~rapidfuzz first (≥95); escalate to LLM on miss~~ | Superseded by ADR 0040, then ADR 0082 | [0026](../decisions/0026-hybrid-routing-rapidfuzz-then-llm.md) *(superseded)* |
| ~~rapidfuzz threshold when LLM enabled~~ | ~~95 (up from 85)~~ | Superseded by ADR 0040, then ADR 0082 | [0027](../decisions/0027-rapidfuzz-threshold-tightening.md) *(superseded)* |
| Parameter resolver | `resolver.py` exports pure `resolve_window(target)` + `resolve_app(target)` functions; module-level `_set_config(cfg)` hook injects fuzzy thresholds once at daemon startup; cache for Start-Menu + AppsFolder populated lazily | Pure-per-call API stays unit-testable; thresholds are a config knob, not a code edit; COM enumeration cost paid once on first `open` | [0042](../decisions/0042-resolver-module-design.md) |
| Primitive catalog | 11 primitives (7 action + 4 perception): `focus`, `type`, `open`, `press`, `wait`, `click`, `scroll`, `read_clipboard`, `get_active_window_title`, `get_cursor_pos`, `ocr_region`. `no_match` removed by ADR 0082. | Minimal orthogonal catalog; composable into any graph plan | [0043](../decisions/0043-nine-verb-primitive-catalog.md), [0075](../decisions/0075-primitives-only-mvp.md) |
| Tool argument source of truth | Python type hints + sidecar TOML `[tool.args.<name>]` sub-tables | Single authoring point; `inspect.signature` derives JSON schema; no extra deps | [0034](../decisions/0034-sig-plus-toml-as-single-source-of-truth.md) |
| Commander skill for argument-bearing tools | Extended interview: args, `settle_ms`, `llm_only`; post-write validator run | Prevents sig↔TOML drift at creation time rather than at daemon start | [0035](../decisions/0035-commander-skill-contract-extension.md) |
| Startup validation | `build_streaming_daemon()` validates sig↔TOML consistency; `--validate` CLI flag for CI | Fail-loud at load time; wrong arg names or missing descriptions crash before any utterance runs | [0036](../decisions/0036-startup-validator-tool-drift.md) |
| ~~LLM warmup~~ | ~~POSTs a real `/v1/chat/completions` to seed KV cache~~ | Superseded by ADR 0082 | [0038](../decisions/0038-llm-router-warmup-real-chat-completion.md) *(superseded)* |

## Window / Focus Primitives

| Decision | Choice | Why | ADR |
|---|---|---|---|
| `SetForegroundWindow` hardening | `AttachThreadInput(foreground_tid, target_tid, True)` + `AllowSetForegroundWindow(ASFW_ANY)` + `GetForegroundWindow()` post-call verification; raises `FocusWindowError` on verified failure | Windows 11 foreground-lockout causes silent failure; chains continued with keystrokes landing in wrong window; verified raise halts plan cleanly | [0037](../decisions/0037-focus-window-attachthreadinput-workaround.md) |
| Focus tool `settle_ms` | `settle_ms = 200` on the `focus` verb (former `focus_browser` / `focus_terminal` / `focus_window` tools collapsed into a single catalog entry — ADR 0043) | Extra 50 ms over previous 150 ms value accounts for Windows 11 paint latency after `AttachThreadInput`-based focus; only applied on success, never after `FocusWindowError` | [0037](../decisions/0037-focus-window-attachthreadinput-workaround.md), [0033](../decisions/0033-per-tool-settle-ms-plus-wait-primitive.md) |

## Daemon Lifecycle

| Decision | Choice | Why | ADR |
|---|---|---|---|
| Single-instance lock | OS-level file locking (`msvcrt`/`fcntl`) + fixed ctypes HANDLE truncation + PID:GUID + cleanup handlers | Advisory file lock auto-released on crash; ctypes `c_void_p` for 64-bit HANDLE correctness | [0039](../decisions/0039-single-instance-lock-hardening.md) |

## Sprite Companion

| Decision | Choice | Why | ADR |
|---|---|---|---|
| Sprite isolation | Separate OS process via SSE, not in-process | Crash isolation; daemon has zero sprite knowledge | [0045](../decisions/0045-sprite-separate-process-via-sse.md) |
| Sprite renderer | pyglet 2.x | Per-pixel alpha, sprite-sheet primitives, HWND access, no Qt bloat | [0046](../decisions/0046-pyglet-over-tkinter-pyqt-web-overlay.md) |
| Character sheet format | Grid PNG + sidecar TOML | Art regen = image gen + TOML edit, no code changes | [0047](../decisions/0047-charsheet-grid-format-sidecar-toml.md) |
| Outbound telemetry | In-daemon EventBus + FastAPI SSE `/events`; events: `session`, `tool_fired`, `miss`, `heartbeat`, `plan_outcome`, `transcript` (ADR 0079) | Single broadcast channel for sprite, future dashboard, analytics | [0048](../decisions/0048-eventbus-sse-outbound-telemetry.md) |
| Miss chimes | Retained alongside sprite | Sprite supplements audio, does not replace it | [0049](../decisions/0049-miss-chimes-retained.md) |
| Pyglet transparent-overlay recipe | `sample_buffers=0` + `glClearColor` in `on_draw` every frame + `glBlendFuncSeparate` + per-sprite `blend_src/dest` + no manual `SetLayeredWindowAttributes` + no `DwmExtendFrameIntoClientArea` + startup `alpha_size` log | Six independent Win11 traps all produce the same opaque-black-background symptom; must be mitigated together or transparency fails | [0050](../decisions/0050-pyglet-transparent-overlay-windows-recipe.md) |
| Command HUD surface | Standalone chat-log overlay rendered in the same pyglet window as the sprite | Glanceable RPG-chat feedback per command; sharing the window avoids a second HWND | [0051](../decisions/0051-command-hud-overlay.md) |
| HUD summarization | ~~Hybrid: rule table covers nine-verb catalog, LLM fallback on errors / unknown verbs~~ Rule-table summarizer deleted; HUD renders raw `tool_fired.name` strings directly. `plan_outcome` with `status="miss"` renders `"no match"`. | Cheap hot path, rich error explanations, bounded worst case | [0052](../decisions/0052-hybrid-rule-llm-summarization.md) *(superseded by ADR 0075)* |
| Multi-monitor docking | 30 Hz `GetCursorPos` poll → `MonitorFromPoint` → `rcWork` → `set_location` + `set_size`; PMv2 awareness mandatory | Sprite + HUD track the cursor; taskbar-excluded docking works on any taskbar edge | [0053](../decisions/0053-sprite-follows-cursor-monitor.md) |
| LLM plan dispatch mode | `Plan.strict=True` halts on first error; `strict=False` continues and records only first failure in `PlanOutcome` | [0054](../decisions/0054-last-primitive.md) |
| Dispatcher continue-on-error | `Plan.strict=False` enables continue-on-error; `strict=True` default preserves existing halt semantics | [0055](../decisions/0055-plan-strict-continue-on-error.md) |

All ADRs live in [`../decisions/`](../decisions/). Add a new row here whenever you add a new ADR.

## Node-Graph Builder

| Decision | Choice | Why | ADR |
|---|---|---|---|
| Node-graph editor library | Drawflow vendored as `drawflow.min.js` + `drawflow.min.css` under `web/static/`; no npm, no build step | Zero runtime deps; plain `<script>` include satisfies ADR 0022 no-SPA rule; deliberate-bump upgrade model | [0062](../decisions/0062-drawflow-vendored-node-graph-editor.md) |
| Graph on-disk format | Canonical JSON schema (`schema_version`, `name`, `kind`, `nodes`, `edges`, `inputs`, `outputs`) with a thin `graph_drawflow.py` adapter converting to/from Drawflow's native export shape | Human-readable, hand-editable, lib-portable; roundtrip fixed-point tests are mandatory | [0063](../decisions/0063-canonical-graph-schema-with-lib-isolated-adapter.md) |
| Graph execution model | `GraphRuntime` walks canonical graphs as DAGs; `LLMRouter.route()` and `Dispatcher.run_plan()` interfaces unchanged; graphs register as `ToolEntry` closures | LLM sees flat `Plan` of named calls; small models cannot reliably author DAGs at runtime; `PlanOutcome` wire format preserved | [0064](../decisions/0064-graph-runtime-supersedes-linear-workflow-steps.md) |
| Primitive return values | `focus`, `open`, `last` return `hwnd: int`; existing void callers unaffected (Python discards unused returns) | Graph runtime needs source values for data wires; no wrapper class overhead; no breaking changes | [0065](../decisions/0065-typed-return-values-on-window-process-primitives.md) |
| Perception primitives | `read_clipboard`, `get_active_window_title`, `get_cursor_pos`, `ocr_region(x,y,w,h)` as new `[perception]` tool group; `winrt` primary, Tesseract fallback | Branch nodes need readable desktop state; leaving perception out removes the main reason for typed data wires | [0066](../decisions/0066-perception-primitives-layer.md) |
| ~~LLM tool-list filtering~~ | ~~`Graph.llm_visible` excluded graphs from `LLMRouter.all_llm_visible()`~~ | Superseded by ADR 0082; `llm_visible` field retained in JSON schema for backward compat but ignored by registrar | [0067](../decisions/0067-per-graph-llm-visible-flag.md) *(superseded)* |
| Commander skill scope | Skill expanded to interview for all three layers (primitive → command → workflow); commands/workflows written as canonical JSON direct file write; no web-UI dependency | Terminal-driven, stateless, offline-capable; hot-reload picks up new files automatically | [0068](../decisions/0068-commander-skill-multi-layer-authoring.md) |
| Builder visual taxonomy | Per-category gradients + shape modifiers (pipeline=amber, command=teal/pill, workflow=violet, control=rose, value=emerald); port colors via `data-port-name` attribute selectors; frontend `normalizeArgs()` fixes pipeline primitive drag bug. Implementation moved to React SPA per ADR 0071; palette layout (`pipeline`+`perception` arrays, `system=true` filter, "Primitives" section name) covered by ADR 0078. | Color+shape achieves legibility with pure CSS; no icon font/SVG sprite; backend `args` dict shape preserved | [0069](../decisions/0069-builder-visual-taxonomy.md) |
| Observability | SQLite-backed Tracer with span tree (`run` / `transcribe` / `llm_call` / `plan` / `tool_call` / `graph` / `node`); live SSE on `trace.*`; web `/page/runs` + Builder overlay; `vc debug` / `vc tail` CLIs; LLM-replay safe, full replay gated. | Single source of truth for per-utterance debugging; UI + CLI + agent API share schema. | [0070](../decisions/0070-observability-pipeline.md) |
| Builder React SPA + n8n-style runs panel | `/page/builder` replaced with React 18.3 + Vite 5 + TypeScript SPA (`web/builder-ui/` → built to `web/static/builder/`); Drawflow replaced by React Flow; Zustand stores (`graphStore`, `runsStore`, `uiStore`); n8n-style runs panel with SSE live tail, span-tree drawer, copy-as-prompt export; 3-bucket error taxonomy (`program`/`wiring`/`infra`) in `observability/errors.py` (`llm` bucket removed by ADR 0082); schema v1→v2 adds `error_category` columns. | HTMX ceiling reached — live SSE merge + client-side filter + drawer + node↔row highlight required real client state; Drawflow imperative API incompatible with OOB swap layering. | [0071](../decisions/0071-builder-react-spa.md) |
| Speak dictation toggle | `_speak_mode` flag replaces `_muted`; audio stream stays open; LLM route gated in `_process_utterance`; `speak` tool synthesizes Right Ctrl via `pynput.Controller`; `system = true` TOML flag hides tool from web UI; fuzzy wake-word exit (`rapidfuzz.fuzz.ratio >= 95, word_count == 1`). Supersedes ADR 0025. | Stream-close-on-mute incurred PortAudio overhead and reopen lag; flag-only gating is zero-cost and eliminates `_drain_utt_q()` and pipeline mute-guard complexity. | [0072](../decisions/0072-speak-dictation-toggle.md) *(superseded by ADR 0075)* |
| Registry-aware VerbRouter | VerbRouter queries the live tool registry at route time; user-authored commands and workflows are matched by name + synonyms before primitive verb rules; longest-match wins; underscore↔space normalised. Extends ADR 0074; partial-supersedes the primitives-only routing claim of ADR 0075. | Lets users invoke their own commands by utterance without a separate synonym mapping layer; registry is already the source of truth for all tool metadata | [0076](../decisions/0076-registry-aware-verb-router.md) |
| `press()` permissive argument parser | Multi-delimiter split (space / `+` / `,`); alias map normalises common key names (e.g. `ctrl→control`, `win→windows`); unknown key names emit WARNING rather than raising; valid subsets still execute | Strict parsing rejected natural utterances like "press ctrl c"; WARNING-not-raise lets partial chords succeed and surfaces surprises in logs without crashing the plan | [0077](../decisions/0077-press-permissive-argument-parser.md) |
| Builder palette partition | Palette sidebar splits into `pipeline` array (action primitives) + `perception` array (perception primitives); `system=true` TOML flag hides internal tools from the palette; section labelled "Primitives". Extends ADR 0069, ADR 0071, ADR 0075. | Separating pipeline and perception groups reduces visual noise; `system=true` keeps housekeeping tools out of the authoring surface without removing them from the registry | [0078](../decisions/0078-builder-palette-pipeline-perception-partition.md) |
| `transcript` SSE event | New `transcript` EventBus event emitted per utterance (raw text + run_id); HUD renders a per-utterance sequence: `transcript` → `tool_fired`/`miss` → `plan_outcome`; sprite state machine consumes event to show "thinking" frame. Extends ADR 0048, ADR 0051. | Closes the feedback gap between speech end and first tool fire; allows HUD to show raw text immediately so users know the daemon heard them | [0079](../decisions/0079-transcript-sse-event.md) |
| Builder graph-settings panel | PropertiesPane empty state (no node selected) renders a `GraphSettingsPanel` editing graph-level `description` + `synonyms` via a chip-style `SynonymsEditor`; round-trips through existing `POST /graph/{name}` (no backend changes); legacy graphs without `synonyms` normalise to `[]` on load; "Edit primitive phrases →" link surfaces the existing `/page/primitives` HTMX editor. Extends ADR 0071, ADR 0076. | Authors can register Whisper mistranscriptions (e.g. `P.A.C.T.` → `paste`) directly from the Builder instead of hand-editing `commands.json`; reuses dead empty-state real estate | [0080](../decisions/0080-builder-graph-settings-panel.md) |
| WASAPI-only audio device resolution | `_resolve_device_by_name()` filters `sd.query_devices()` to the Windows WASAPI host API only; `[audio].device_name` is the authoritative identity, `[audio].device` int is a non-authoritative cache validated against `hostapi == WASAPI` on the fast path. DirectSound / MME / WDM-KS duplicates of the same device name are skipped. Resolver exceptions never wipe the saved index. | DirectSound copies of the same physical mic kept winning the name match and then throwing `PaErrorCode -9999 / DirectSound -2005401480` when Windows reshuffled the device list. WASAPI is the modern, stable Windows capture path. | [0081](../decisions/0081-wasapi-only-audio-device-resolution.md) |
| Remove LLM routing path | `LLMRouter`, Merlin toggle, `prompt_template.txt`, `no_match` tool, `LLMConfig`, `all_llm_visible()`, Prompt Inspector, `replay-llm` endpoint, `llm` error bucket all deleted. `[llm]` config section silently dropped with deprecation warning. `llm_visible` graph field retained in JSON schema (backward compat) but ignored by registrar. | LLM path was never activated in daily use; LM Studio introduced latency + service dependency; VerbRouter + user-authored graphs cover 100% of realistic utterances. | [0082](../decisions/0082-remove-llm-routing-path.md) |
| Bare-primitive picker framework | Bare verb (e.g. `focus` with no argument) opens a centred numbered modal of MRU targets; next utterance coerced to int picks one. Pluggable via `@bare_picker(verb)` on a global `BarePickerRegistry`. MRU ring buffer driven by `WinEventHook(EVENT_SYSTEM_FOREGROUND)`. Daemon owns state; sprite renders modal via SSE `picker.open` / `picker.close`. Synthetic tool name `__picker.open` intercepted before dispatch. | Bare verbs were misses; modal disambiguation is cheaper and more general than hardcoding per-verb branches; framework generalises to future `tab`/`open` pickers without new infra. | [0083](../decisions/0083-bare-primitive-picker.md) |
| `tabs` bare picker via Chromium UIA | Saying "tabs." opens a numbered modal of the foreground Chromium browser's page tabs; saying the number activates that tab via `SelectionItemPattern.Select()` on the matched `TabItemControl`. Process-name allowlist (Chrome/Edge/Brave/Comet/…) excludes Electron apps with incidental TabControls. TabItems collected as `TabControl` descendants (Chromium nests them as `TabControl > Pane > Pane > TabItem`, not direct children). Empty list ⇒ miss-chime. Single-line cards (page title in `app` slot, `title` empty). Picker-only `tabs(_browser_hwnd, _tab_index, _tab_title)` primitive, `internal=true`. Bare-only verb rule. Builds on ADR 0083. | Sakib wants tabs-by-number in his browser; reuses entire picker framework with one new provider + one primitive; UIA works without DevTools / debugger flags. | [0084](../decisions/0084-tabs-bare-picker-chromium-uia.md) |
| Chain meta-verb primitive | `chain <tok1> <tok2> … <tokN>` expands into a multi-step `Plan` with synthetic `wait(ms=255)` steps (`ToolCall.internal=True`) interleaved between user steps. Dispatcher suppresses `tool_fired` events and `FeedbackSink` calls for internal steps; tracer spans still record them. Head words `chain`/`chained`/`chains` have highest router precedence. Forbidden tokens: `open`, `type`, `press`, `wait`, `focus`, `tabs`, `chain`. Longest-match wins; registered commands beat primitives on tie. `Plan.strict=True`. New module `voice_commander.chain`; `VerbRouter` accepts optional `chain_parser`. Router-level only — invisible to `/graph/palette` and Builder UI. | Firing several short commands in one breath without authoring a workflow graph; 255 ms inter-step gap keeps Windows from dropping keypresses between steps. | [0085](../decisions/0085-chain-primitive.md) |
| Dictation mode / remote transcription | Bare "dictate" utterance or Right Ctrl (`dictation_key`, default `ctrl_r`) during an active session enters dictation sub-state; VAD audio accumulates in `DictationSession`. Exit: standalone end word (default `done`) or second Right Ctrl press. Concatenated audio encoded to 16 kHz mono WAV and POSTed to remote whisper.cpp `/inference` endpoint (`[dictation] endpoint`). Result inserted via clipboard round-trip (`Ctrl+V`). One-slot retention (`outputs/dictation/last.wav` + `last.txt`); web `/page/dictation` re-transcribe. Sprite shows `● DICTATING` badge. In-session mute toggle (ADR 0025) removed; `HotkeyConfig.mute_key` renamed `dictation_key`. Tool catalogue unchanged at 11 primitives. **Transcription transport superseded by ADR 0092 (streaming WebSocket path replaces batch POST).** | Lets users dictate free-form text inside a voice session using a higher-accuracy remote model, without an external dictation app or stream-close/reopen churn. | [0086](../decisions/0086-dictation-mode.md) *(supersedes ADR 0025; transcription transport superseded by ADR 0092)* |
| Elements mode / UIA element-click overlay | Saying "element"/"elements" during an active session scans the foreground window's UI Automation tree on a worker thread; clickable controls (buttons, hyperlinks, menu/list/tab/tree items, checkboxes, radio buttons, combo boxes, edits, split buttons) are filtered, deduped, sorted into reading order and drawn as a numbered click-through overlay by the sprite via `elements.show` / `elements.hide` SSE events. Next utterance names the number ⇒ coordinate left-click (`pyautogui`) on that element's centre; any non-number utterance or an 8 s timeout dismisses. One-shot lifecycle (mode exits after one click). New `voice_commander.elements` package (scanner, `ElementsSession` state machine, number parser, clicker, desktop helpers) + `voice_sprite/elements_overlay.py`; `[elements]` config section; new `uiautomation` dependency. Scanner runs off-thread so the pipeline never blocks. Browser page content is best-effort (needs `--force-renderer-accessibility`); native UI works unconditionally. No new LLM-visible primitive. | Lets users click arbitrary on-screen UI — native apps and browser page content — by voice, without authoring a command graph per target; generalises click-target selection beyond the bare-verb pickers. | [0087](../decisions/0087-elements-mode-uia-overlay.md) |
| Dictation custom vocabulary | `vocab.json` with three layers (prompt biasing, corrections, commands); `VocabStore`, `postprocess.py` pure functions; hot-reload on every dictation; no new config keys or primitives | Domain jargon spelled correctly; mistranscriptions corrected deterministically; voice-controlled formatting without new primitives; tool catalogue stays at 11. | [0088](../decisions/0088-dictation-custom-vocabulary.md) |
| Dictation hotkey-end sentinel, 50 ms debounce, spoken cancel | `_DICTATION_WAKE` sentinel enqueued by `on_dictation_toggle` wakes the pipeline thread from `get(timeout=None)` so hotkey-end finalizes within ≤ 250 ms even with no trailing speech; `HotkeyController._last_fire` dict drops any release within 50 ms of the same key's last dispatch (bounce rejection); `DictationSession.cancel_word` (default `"cancel"`) returns `UtteranceKind="cancel"`, pipeline calls pre-existing `cancel()`, no POST/paste; sprite sets `cancelled_cue` on `dictation.end {reason:"cancel"}`. | Eliminates hotkey-end pipeline race; eliminates driver-bounce double-toggle; gives users a no-paste escape hatch; zero new infra — reuses `cancel()` and the EventBus `dictation.end` event. Tool catalogue unchanged at 11. | [0089](../decisions/0089-dictation-hotkey-sentinel-cancel-debounce.md) |
| Dictation hotkey opens own session | Right Ctrl with no Scroll Lock session open calls `_open_voice_session()`, sets `_session_opened_by_dictation=True`, starts dictation; all three end paths submit `_end_owned_session_if_needed` to `_dictation_executor` BEFORE `_finalize_dictation` (close-first ordering); `_TIMEOUT_S` reduced to 30 s; `shutdown()` cancels active dictation directly before executor teardown (ADR 0090) | Right Ctrl becomes a complete transcribe toggle; recording-stop chime fires before paste; 30 s timeout prevents executor stall | [0090](../decisions/0090-dictation-hotkey-opens-session.md) |
| ~~Streaming dictation (experimental)~~ | ~~Isolated `dictation_stream` package: hybrid chunking via reused `VADGate`, WebSocket streaming to `/ws/transcribe`, `LocalAgreement` word stabilisation, accumulate-then-paste with a `transform()` seam for a future LLM step. Standalone (`python -m voice_commander.dictation_stream`), Right Ctrl toggle. Not wired into the daemon.~~ | ~~Streaming transcription with progressive, context-carrying decoding; zero risk to shipped batch dictation~~ | [0091](../decisions/0091-streaming-dictation-experiment.md) *(superseded by ADR 0096)* |
| ~~Streaming dictation integration~~ | ~~`DictationSession` streams each VAD utterance to a whisper WebSocket (`/ws/transcribe`, `[dictation] ws_url`); `LocalAgreement` stabilises words across chunk boundaries; `DictationSession.finish()` returns the stabilised transcript; batch `/inference` POST path and `last.wav` / re-transcribe removed. `dictation_stream/` package deleted. Daemon lifecycle wiring (ADR 0090) unchanged.~~ | ~~Real progressive transcription with cross-chunk decoding context; eliminates full-utterance buffer and batch latency; unifies dictation code to one path~~ | [0092](../decisions/0092-streaming-dictation-integration.md) *(superseded by ADR 0096; lifecycle wiring reaffirmed)* |
| Transcription proxy endpoint | `[dictation] ws_url` default moved from the raw whisper server (`:8765/ws/transcribe`) to a protocol-compatible WebSocket proxy (`:8767/ws/transcribe`). Proxy LLM-cleans the final `done` transcript before returning it; silent raw-text fallback if the LLM is unavailable. One-key config change — caller-side protocol simplified by ADR 0096 (no config frame, no partial frames). | Punctuated/cleaned dictation text without a daemon-side LLM dependency or latency; voice-commander stays local-first; proxy fallback degrades gracefully. | [0093](../decisions/0093-transcription-proxy-endpoint.md) *(amends transcription endpoint of ADR 0092)* |
| Consume `done` frame for LLM-cleaned transcript | `stream_transcribe` reads the server's single `done` frame after sending `{"type":"end"}`; returns `done.text` (`str`, possibly `""`) or `None` on timeout/close/error. `DictationSession.finish()` returns `done.text` when not `None`; returns `None` (empty-string paste + miss chime) when no `done` frame arrives. `done_timeout_s=60.0` (increased from 15 s to cover the longer Whisper + LLM latency). `raw_transcript` field no longer sent on the end frame (server does the only decode). | LLM-cleaned text is pasted; `done`-frame contract preserved across ADR 0096 pivot. | [0094](../decisions/0094-consume-done-frame.md) *(reaffirmed by ADR 0096)* |
| ~~Streaming-window dictation~~ | ~~Growing-window re-decode replaces per-chunk streaming; `DictationWindow` (frame-tap-fed 16 kHz buffer) emits whole-window WAVs every `window_step_ms`; LocalAgreement-2 commits longest agreeing prefix of consecutive hypotheses with absolute end-times; VAD demoted to endpointing; `raw_transcript` on end frame for proxy LLM-clean; `window_cap_ms` bounds uncommitted buffer; degrades gracefully when server omits `segments`.~~ | ~~Eliminates pause-fragmentation / stray `...` / boundary word loss; Whisper sees growing context instead of isolated short clips; coherent punctuated output.~~ | [0095](../decisions/0095-streaming-window-dictation.md) *(superseded by ADR 0096)* |
| Server-side dictation (major pivot) | Daemon is a thin PCM transport + UI coordinator. Raw 16 kHz mono float32 PCM bytes streamed as binary WebSocket frames (no WAV headers, no config frame, no partial frames). On end-word/hotkey-end/cancel: daemon sends `{"type":"end"}`, publishes `dictation.processing`, awaits `{"type":"done","text":...,"raw":...}`. Server accumulates raw bytes, calls `transcribe(audio, vad_filter=True)` once, LLM-cleans, returns done frame. Client-side: `LocalAgreement`, `DictationWindow`, and frame tap deleted entirely. `max_dictation_s=300` (daemon cap) replaces `window_step_ms`/`window_cap_ms`. `done_timeout_s=60`. Sprite gains `processing` state; HUD is quiet during dictation. | Eliminates the entire class of LA-2 index-math / trim-reset / stray-partial bugs; simpler wire protocol (3 frame types instead of 7); one Whisper decode per dictation. | [0096](../decisions/0096-server-side-dictation.md) *(supersedes ADR 0091, 0092, 0095; reaffirms ADR 0094)* |
